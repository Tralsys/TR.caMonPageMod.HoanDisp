<ResourceDictionary
		xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
		xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
		xmlns:local="clr-namespace:TR.caMonPageMod.HoanDisp">

	<local:TextWritingDirectionConverter x:Key="TWDirectionConv"/>

	<Style TargetType="{x:Type local:M_HoanLamp}">
		<Setter Property="Template">
			<Setter.Value>
				<ControlTemplate TargetType="{x:Type local:M_HoanLamp}">
					<Border x:Name="border"
									Background="{TemplateBinding Background}"
									Width="{TemplateBinding Width}"
									Height="{TemplateBinding Height}"
									BorderBrush="{x:Null}">
						<TextBlock x:Name="tb" Text="{TemplateBinding Text, Converter={StaticResource TWDirectionConv}}" HorizontalAlignment="Center" Margin="{TemplateBinding Padding}"/>
					</Border>
					
					<ControlTemplate.Triggers>
						<Trigger Property="IsLighting" Value="True">
							<Setter TargetName="tb" Property="Foreground" Value="Black"/>
						</Trigger>
						<Trigger Property="IsLighting" Value="False">
							<Setter TargetName="border" Property="Background" Value="#202020"/>
							<Setter TargetName="tb" Property="Foreground" Value="White"/>
						</Trigger>
					</ControlTemplate.Triggers>
				</ControlTemplate>
			</Setter.Value>
		</Setter>
	</Style>
</ResourceDictionary>
